#include <stdio.h>
#define pi 3.14

int main(){

float r;
printf("enter value of r:");
scanf("%f", &r);

printf("area of circle is %f \n", pi*r*r);
printf("circumference of circle is %f", 2*pi*r);

   return 0;
}
